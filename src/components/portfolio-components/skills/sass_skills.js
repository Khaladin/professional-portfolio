import React, {Component} from 'react';

class SassSkills extends Component {
  render() {
    return(
      <div class="skills">
        <p class="skills__sass">
          Sass is the newest language I have invested time into. I enjoy the BEM naming system
          because I was never able to come up with a good naming system before I discovered it.
          I also Like it because it uses organization just like React does, making the learning for it much easier.
        </p>
      </div>
    );
  }
}

export default SassSkills;
